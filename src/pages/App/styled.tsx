import styled from 'styled-components';

export const AppContainer = styled.div`
  height: calc(100% - var(--header-size) - 40px);
  max-width: var(--screen-max-width);
  margin: 0 auto;
  margin-top: 40px;
`;
