export enum MessageEnum {
  NEW = 'new',
  MEMBERS = 'members',
  MESSAGE = 'message',
  OUT = 'out',
}

export enum AgendaStatus {
  PREPARE = 'prepare',
  PROGRESS = 'progress',
}

export enum LoginStatus {
  LoggedIn,
  NotLoggedIn,
  Pending,
}

export enum AwayStatus {
  Entered,
  Vacant,
  Staying,
}
